# 功能更新摘要 (2026-08-10)

## ✨ 新增功能

### 1. 可調節起點金額
- **功能**：用戶可自定義初始投資金額（預設 1000）
- **位置**：新增比較組表單右側
- **實現**：
  - `startValue` 狀態欄位（預設 1000）
  - 數字輸入框，最小值 1，步長 100
  - 數據保存至 D1（ComparisonGroup.startValue）

### 2. 改進績效圖表
- **線條粗度**：2 → 3px（更清晰可見）
- **線條效果**：加入 strokeLinecap="round" 和 strokeLinejoin="round"
- **顏色方案**：
  - 舊：4 種相似的日系配色（低對比度）
  - 新：8 種高對比顏色（深綠、紅、藍、橙、紫、青、粉、橄欖）
  - Legend 指示器：從 0.5px 高度 → 1px 高度 + 圓角

### 3. 風險係數指標
新增兩個重要風險調整後報酬指標：

#### Calmar 比率（Calmar Ratio）
```
Calmar = 年化報酬 / |最大回撤|
```
- **含義**：衡量每承受 1% 回撤能獲得多少報酬
- **應用**：識別風險控制良好的資產
- **優勢**：比 Sharpe 更重視回撤控制

#### Sortino 比率（Sortino Ratio）
```
Sortino = 年化報酬 / 下側偏差
```
- **含義**：只計算負報酬的波動度（下側風險）
- **應用**：更準確反映投資者感受的風險
- **優勢**：不懲罰上行波動，比 Sharpe 更合理

## 📊 技術變更

### 新增/修改文件
| 文件 | 變更 |
|------|------|
| `lib/types.ts` | BacktestMetrics 新增 `calmarRatio`, `sortinoRatio`；ComparisonGroup 新增 `startValue?` |
| `lib/backtest.ts` | 新增 `downsideDeviation()` 函數；計算 Calmar 和 Sortino 指標 |
| `components/PerformanceChart.tsx` | 線條粗度 2→3px；8 色高對比配色；Legend 優化 |
| `app/page.tsx` | 新增起點金額輸入框；表格新增 2 列（Calmar、Sortino） |
| `lib/i18n.ts` | 新增中英雙語標籤 |
| `tests/backtest.test.ts` | 更新測試用例包含新指標 |

### 代碼質量
✅ TypeScript 檢查通過  
✅ 測試用例已更新  
✅ 雙語支持完善  

## 🎨 UI 變化

### 新增比較組表單
```
┌─────────────────────────────────────┐
│ 組合名稱 [............]              │
│ 起點金額 [1000        ]              │
└─────────────────────────────────────┘
```

### 績效表格（新增 2 列）
```
┌─────────────────────────────────────────────────────────────────┐
│ 標的 | 累積報酬 | 年化報酬 | 波動度 | 最大回撤 | 夏普 | Calmar | Sortino │
├─────┼──────────┼──────────┼────────┼─────────┼──────┼────────┼─────────┤
│ QQQ │  85.32%  │  12.45%  │ 18.92% │ -42.15% │ 0.51 │  0.30  │   0.42  │
└─────────────────────────────────────────────────────────────────┘
```

### 圖表線條
- 線條更粗（3px），顏色差異明顯
- Legend 色塊更清晰（高度 1px，帶圓角）

## 📈 使用場景

### 場景 1：小額投資驗證
用戶想驗證 100 元投資某組合的表現：
```
1. 起點金額改為 100
2. 執行回測
3. 查看最終價值（小數點精確）
```

### 場景 2：風險評估
用戶想找既獲利又風險低的資產：
```
1. 比較多個 ETF
2. 查看 Calmar 和 Sortino 排序
3. 選擇高風險調整報酬的標的
```

### 場景 3：圖表分析
用戶想清楚看出各資產表現差異：
```
1. 8 種高對比顏色自動分配
2. 3px 粗線條清晰可見
3. 快速識別勝者與落後者
```

## 🔍 指標解釋

| 指標 | 公式 | 好值 | 用途 |
|------|------|------|------|
| Sharpe | (年化報酬 - 無風險率) / 波動度 | > 1.0 | 風險調整報酬（傳統） |
| Calmar | 年化報酬 / \|最大回撤\| | > 1.0 | 回撤控制能力 |
| Sortino | 年化報酬 / 下側偏差 | > 1.0 | 下行風險調整報酬 |

## ✅ 測試清單

- [x] 起點金額輸入可用（1-999999）
- [x] 新指標計算正確
- [x] 表格顯示新列無溢出
- [x] 圖表線條粗度明顯
- [x] 顏色對比充分
- [x] Legend 清晰
- [x] 雙語標籤完整
- [x] TypeScript 無錯誤
- [x] 舊數據相容（startValue 可選）

## 🚀 下一步優化

- [ ] 起點金額記憶（下次開啟默認選擇）
- [ ] 新增 VaR（Value at Risk）指標
- [ ] 圖表懸停顯示具體數值
- [ ] 導出報表功能

## 📝 部署說明

代碼已完成，無需額外配置。

**本地測試**：
```bash
npm run dev
# 訪問 http://localhost:3000
# 新增比較組，修改起點金額，執行回測
```

**部署**：
```bash
# 在你的 Mac 執行
npm run deploy
```

---

**版本**：v0.2.0  
**日期**：2026-08-10  
**狀態**：✅ 準備就緒
